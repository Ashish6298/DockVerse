# Server Configuration
PORT=5000
NODE_ENV=development

# Docker Connection (Optional - auto-detected by OS by default)
# DOCKER_SOCKET_PATH=//./pipe/docker_engine
# DOCKER_HOST=
# DOCKER_PORT=

# MongoDB URI (Future phases for metadata storage)
MONGO_URI=mongodb://localhost:27017/dockverse

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
