{
  "name": "dockverse-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "DockVerse - Docker Developer Workspace Center",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
